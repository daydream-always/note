# 23 82 删除排序链表中的重复元素 II（迭代版）

```Java
class Solution {
    public ListNode deleteDuplicates(ListNode head) {

        ListNode dummy = new ListNode(-1);
        dummy.next = head;
        ListNode cur = dummy;

        while(cur.next!=null && cur.next.next!=null){

            if(cur.next.val == cur.next.next.val){
                
                int value = cur.next.val;

                while(cur.next!=null && cur.next.val == value){
                    cur.next = cur.next.next;
                }

            }else{
                cur = cur.next;
            }
        }
        return dummy.next;
    }
}
```



```java
class Solution {
    public ListNode deleteDuplicates(ListNode head) {

        ListNode dummy = new ListNode(-1);
        dummy.next = head;
        ListNode cur = dummy;
		//下一个和下下一个
        while(cur.next!=null && cur.next.next!=null){
			//下一个和下下一个相同
            if(cur.next.val == cur.next.next.val){
                //删除的方法是记录该值
                int value = cur.next.val;
				//与目标值相同，跳过该值，让cur的next，指向下下一个
                while(cur.next!=null && cur.next.val == value){
                    cur.next = cur.next.next;
                }
			//下一个和下下一个不同，指针后移，12->23>34->45依次判断
            }else{
                cur = cur.next;
            }
        }
        return dummy.next;
    }
}

/**
cur.next!=null && cur.next.next!=null，避免cur.next.next为空情况
cur.next!=null，避免指向链表末尾
*/
```



