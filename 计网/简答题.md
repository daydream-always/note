# 简答题
2.试述具有**五层协议的网络体系结构**的要点，包括各层的主要功能。

(1) 物理层：在物理媒体上传送比特流。

(2) 数据链路层：在**两个相邻结点间**（**主机和路由器**或路由器和路由器之间）的**链路上传送以帧为单位的数据**。

(3) 网络层：负责将分组从源主机（按照合适的路由）通过中间若干路由器的转发传送到目的主机。

(4) 运输层：负责**主机中两个进程之间的逻辑通信（端到端通信）**。

(5) 应用层：通过**应用进程间的交互**来实现特定网络应用，直接为用户或应用进程提供特定的应用服务


4.为什么要使用**信道复用技术**？常用的信道复用技术有哪些？

许多用户可以通过复用技术共同**使用一个共享的信道来进行通信**。
当网络中传输媒体的传输容量大于单一信道传输的通信量时，可利用复用技术在一条物理线路上建立多条通信信道来**充分利用传输媒体的带宽。**


7.简述**TCP和UDP**的主要区别。
TCP提供的是面向连接、**可靠字的字节流服务**
UDP提供的是无连接、**不可靠的数据报服务**


8.简述**TCP流量控制和拥塞控制**的不同。

流量控制解决因发送方发送数据太快而导致接收方来不及接收使接收方缓存溢出的问题。

流量控制的基本方法就接收方**根据自己的接收能力控制发送方的发送速率。**

TCP采用接收方控制发送方发送窗口大小的方法来实现在TCP连接上的流量控制。

拥塞控制就是防止过多的数据注入到网络中，这样可以使网络中的路由器或链路不致过载。

TCP的发送方维持一个叫做拥塞窗口的状态变量。

拥塞窗口的大小取决于网络的拥塞程度，当网络拥塞时减小拥塞窗口的大小，控制TCP发送方的发送速率。

TCP发送方的发送窗口大小取接收窗口和拥塞窗口的最小值。


9.举例说明域名解析的过程。**域名服务器中的高速缓存**的作用是什么？

假设用户在浏览器中输入网址 “www.qtnu.edu.cn”，想要访问这个网站。需经过以下解析过程：

本地缓存查询

操作系统缓存查询

本地 DNS 服务器查询

根 DNS 服务器查询

顶级域名服务器查询

权威 DNS 服务器查询

返回 IP 地址给浏览器。

为了提高DNS查询效率，并减轻根域名服务器的负荷和减少因特网上的DNS查询报文数量，在域名服务器中广泛地使用了高速缓存（有时也称为高速缓存域名服务器）。

高速缓存用来**存放最近查询过的域名**以及**从何处获得域名映射信息的记录**。


11.简述**DHCP**的工作过程。为什么要使用**广播**？

DHCP客户广播**DHCP服务器发现报文**。

DHCP服务器应答**DHCP提供报文**。

客户机发送**DHCP请求报文**。

服务器应答**DHCP确认报文**。

使用广播发送DHCP服务器发现报文是因为客户不知道服务器的IP地址，但又想与之通信。


12.**对称密钥**密码体制与**公钥**密码体制的特点各如何？各有何优缺点？

  对称密钥密码体制是一种**加密密钥与解密密钥相同**的密码体制。

在对称钥密系统中，两个参与者要共享同一个秘密密钥，这给密钥的管理和更换都带来了极大的不便，通常需要使用复杂的密钥分发中心KDC (Key Distribution Center)来解决该问题。

然而采用公钥密码体制可以比较容易地解决这个问题。

  公钥密码体制使用**不同的加密密钥与解密密钥**

加密密钥（即公钥）PK是公开信息，而解密密钥（即私钥）SK是需要保密的

由于加密密钥不能用来解密，并且从加密密钥不能推导出解密密钥，因此加密密钥可以公开。

例如，参与者A可以在报纸上公布自己的加密密钥（即公钥），而解密密钥（即私钥）自己秘密保存。

**任何参与者都可以获得该公钥并用来加密发送给参与者A的信息，而该信息只能由A解密。**

可见采用公钥密码体制更易解决密钥分发的问题。

  公钥密码体制有许多很好的特性，使得它不仅可以用于加密，还可以很方便地用于鉴别和数字签名。

但是，公钥密码算法比对称密码算法要慢好几个数量级。

因此，对称密码被用于绝大部分加密，而公钥密码则通常用于会话密钥的建立。


13.**IPSec**有哪两种运行方式？请简述他们的区别。

IPSec可以以两种不同的方式运行：**传输方式和隧道方式**。

在传输方式下，IPSec保护运输层交给网络层传递的内容，即只保护IP数据报的有效载荷，而不保护IP数据报的首部。

**传输方式通常用于主机到主机的数据保护。**

在隧道方式下，IPSec保护包括IP首部在内的整个IP数据报，为了对整个IP数据报进行鉴别或加密，要为该IP数据报增加一个新的IP首部，而将原IP数据报作为有效载荷进行保护。

**隧道方式通常用于两个路由器之间，或一个主机与一个路由器之间。**


14.试述**防火墙**的基本工作原理和所提供的功能。

防火墙是把一个组织的内部网络与其他网络（通常就是因特网）隔离开的软件和硬件的组合。

根据访问控制策略，它允许一些分组通过，而禁止另一些分组通过。访问控制策略由使用防火墙的组织根据自己的安全需要自行制订。

防火墙作为一种**访问控制技术**，通过**严格控制进出网络边界的分组**，禁止任何不必要的通信，从而减少潜在入侵的发生，尽可能**降低内网遭受外网攻击的安全风险**。


15.**入侵检测方法**一般可以分为哪两种？它们之间的区别是什么？

入侵检测方法一般可以分为**基于特征的入侵检测**和**基于异常的入侵检测**两种。

  基于特征的IDS维护一个所有已知攻击标志性特征的数据库。

当发现有与某种攻击特征匹配的分组或分组序列时，则认为可能检测到某种入侵行为。

**这些特征和规则通常由网络安全专家生成，机构的网络管理员定制并将其加入到数据库中。基于特征的IDS只能检测已知攻击，对于未知攻击则束手无策。**

  基于异常的IDS通过观察正常运行的网络流量，学习正常流量的统计特性和规律，**当检测到网络中流量某种统计规律不符合正常情况时，则认为可能发生了入侵行为。**


16.简述计算机网络的主要功能。

(1)**数据通信**功能:计算机网络最基本的功能是**实现计算机之间的数据传输**。

(2)**资源共享**功能:网络中的用户可以**访问共享的存储设备**，提高设备的利用率。

(3)**分布式处理**功能:提高处理速度和效率。

(4)提高系统可靠性功能:通过计算机网络可以实现数据和设备的冗余备份。


17.什么是**大数据**？大数据有哪些基本特征？

  大数据是指无法在一定时间范围内用常规软件工具进行捕捉、管理和处理的数据集合。

大数据的基本特征：**(1)数据量大；****(2)数据类型多样；**(3)处理速度快；(4)价值密度低；
**(5)数据质量不确定。**


18.什么是**云计算**？云计算有哪些特点？

  云计算是一种**通过互联网提供计算服务**（包括服务器、存储、数据库、网络、软件、分析等）的模式。

这些服务可以根据用户的需求进行快速提供和释放，使用户无需自己构建和维护复杂的计算机硬件和软件系统。

特点：

(1)按需自助服务：用户可以根据自己的需求，自行获取计算资源。

(2)广泛的网络访问：用户可以在任何有网络连接的地方使用云计算服务。

(3)资源池化：服务提供商将计算资源整合到资源池中，多个用户共享这些资源。

(4)快速弹性：系统能够根据用户的需求快速地扩展或收缩计算资源。

(5)可计量服务：服务提供商可以对用户使用的计算资源进行精确的计量和计费。


20.什么是**虚拟局域网**？它有什么特点？

  虚拟局域网（VLAN）是一种**将物理局域网划分为多个逻辑上独立的虚拟网络**的技术。

它**通过在网络交换机上构建多个虚拟网络**，实现逻辑上的隔离和独立。

特点：

(1)提高网络安全性；

(2)增强网络管理效率；

**(3)减少广播风暴影响**；

(4)突破地理位置局限；

(5)灵活的扩展性；


22.什么是**移动IP技术**？

移动 IP 技术是一种允许移动节点（如移动电话、笔记本电脑等移动设备）在不同网络之间移动时仍能保持其原有通信连接（如 **IP 地址不变**）的网络技术。

它使得移动设备能够在不中断正在进行的通信服务的情况下，从一个网络接入点移动到另一个网络接入点

就像**移动用户在移动过程中更换不同的基站接入网络**，**但通话或数据传输不会中断一样**。


24.简述**调制解调器**的基本工作原理。

调制解调器（Modem）通过调制技术，将数字信号的信息加载到高频模拟信号上。

这样，原本的数字信号就被转换为可以在模拟通信线路上传输的模拟信号。

**接收端**的调制解调器会根据发送端采用的调制方式进行相应的解调。

**将模拟信号还原为数字信号，以便数字设备（如计算机）能够理解和处理这些信号。**


25.简述**数字签名**的基本原理。

数字签名是一种用于验证数字消息或文档来源和完整性的技术。基本原理：

(1)密钥生成：发送者首先需要生成一对密钥，即公钥和私钥，私钥由发送者 A 秘密保存，公钥可以公开给任何需要验证签名的接收者。

(2)签名过程：A 使用自己的私钥对要发送的消息（假设为 M）进行加密操作，这个加密后的信息就是数字签名。

(3)验证过程：B 收到消息，首先使用 A 的公钥对数字签名进行解密操作，，B 比较这两个消息摘要，

如果它们完全相同，就说明消息 M 在传输过程中未被篡改，并且消息确实是由 A 发送的。


26.什么是**通信子网和资源子网**?

  通信子网是**计算机网络中负责数据通信的部分**，主要功能是把消息从一台主机传输到另一台主机。

它由通信线路（如双绞线、光纤、微波等）、网络设备（如路由器、交换机、调制解调器等）以及相关的通信协议构成。

  资源子网是**计算机网络中面向用户的部分**，主要负责提供各种网络资源和服务，包括**计算机硬件、软件、数据等资源**。

它由服务器、用户终端（如个人计算机、智能手机等）以及各种应用程序和数据库组成。


27.简述目前网络面临的主要威胁和网络安全的重要性。

网络面临的主要威胁

(1)网络攻击

**(2)数据泄露和窃取**

(3)网络监听和嗅探

网络安全的重要性

**（1）保护个人隐私:**

**（2）维护企业利益:**

**（3）保障国家安全:**

（4）确保网络服务的正常运行:













