1.提高链路速率意味着降低了信道的传播时延。（×）

2.在链路上产生的传播时延与链路的带宽无关。（√）

3.跨越网络提供主机到主机的数据通信的问题属于运输层的功能。（×）

4.发送时延是分组的第一个比特从发送方发出到该比特到达接收方之间的时间。（×）

5.由于动态分配通信带宽和其他通信资源，分组交换能更好更高效地共享资源。（√）

6.采用分组交换在发送数据前可以不必先建立连接，发送突发数据更迅速，因此不会出现网络拥塞。（×）

7.DSL和电话网拨号接入技术都要通过电话网经过电话交换机连接到ISP的路由器的。（×）

8.通过ADSL上网的同时可以利用同一电话线打电话。（√）

9.信道复用技术可以将多路信号复用到同一条传输线路上进行传输，而不会混淆，因此能将该传输线路的带宽成倍增加。（×）

10.电路交换一定是有连接的，而分组交换一定是无连接的。（×）

11.用加密技术来保证信息的机密性的关键是保证加密算法不被泄漏给非授权用户。（×）

12.TCP协议提供可靠的面向连接的虚电路服务而UDP协议提供不可靠的无连接的数据报服务。（√）

13.高质量视频传输属于能容忍少量数据丢失的网络应用。（√）

14.交换机不能隔离广播域。（√）

15.DNS是用来将一个IP地址解析为硬件地址的网络应用。（×）

16.用Ping命令可以测试两台主机间的路由。（×）

17.不同VLAN间的计算机即使连在同一台交换机上也不能直接通信。 （√）

18.可靠数据传输是指传输的数据不会被非授权用户窃取或篡改。（×）

19.IPv6将地址从IPv4的32位增大到了128位。（√）

20.在链路上产生的传播时延与链路的带宽无关。（√）

21.以太网具有碰撞检测和重传功能，因此提供的是可靠数据传输服务。（×）

22.在因特网中，当计算机A要与计算机B通信时，A发送给B的IP报文的目的IP地址一定是B的IP地址，该IP报文被封装在链路层帧中，但链路层目的地址不一定是B的物理地址。（√）

23.路由器负责转发分组，但不知道转发的是UDP报文还是TCP报文段。（√）

24.当路由器收到一个IP报文时，启动选路算法计算到目的地址的“最佳”路径然后将报文从通向该路径的接口转发出去。（×）

25.TCP的确认表示数据已正确收到并已交付给应用程序。（×）

26.使用UDP传输报文是不可靠的，因此有可能让接收方收到内容错误的报文。（×）

27.全球目前有十几个根域名服务器，世界上任何一个联网计算机的IP地址都可以在其中至少一个根域名服务器的数据库中直接查询得到。（×）

28.路由器在转发分组时不会改变分组的MAC地址和IP地址。（×）

29.分组交换可以是无连接的，也可以是有连接的。（  ×  ）

30.全1的IP地址是向整个因特网进行广播的一种地址。（  ×  ）

31.IP数据报在传输的过程中，其首部字段的内容不会发生改变。（  ×  ）

32.传输时延是分组的第一个比特从发送方发出到该比特到达接收方之间的时间。（  ×  ）

33.双绞线由两个具有绝缘保护层的铜导线按一定密度互相绞在一起组成，这样可以增加强度，不易拉断。（  ×  ）

34.路由器能够隔离广播风暴。（  √  ）

35.PPP协议是提供可靠传输服务的数据链路层协议，主要用于点对点链路。（  ×  ）

36.由于GBN协议采用累积确认方式，因此当某个确认分组丢失时，不一定会导致发送方重传。（  √  ）

37.DHCP协议完成IP地址到MAC地址之间的转换。（  ×  ）

38.用traceroute命令可以测试两台主机间的分组转发路径。（√）

39.只要路由器的交换速率足够快，就不会出现网络拥塞。（×）

40.在按组织模式划分的域名中，“edu”表示教育机构。（ √ ）

41.琼台师范学院校园网是由多个局域网互连组成，因此它是广域网。（ × ）

42.为了保障网络安全，防止外部网对内部网的侵犯，一般需要在内部网和外部公共网之间设置防火墙。（ √ ）

43.面向连接的服务提供可靠的传输服务，故面向连接的服务优于无连接的服务。

（ × ）

44.在网络协议的数据链路层上，信息是以二进制位流的格式传送。（ × ）

 

45.标准的B类IP地址使用16位二进制数表示主机号。（  √ ）

46.TCP/IP 协议簇中，IP 协议提供可靠的、面向连接的服务。（ × ）

47.路由器工作在网络层，能够隔离广播域。（ √ ）

48.网络拓扑结构中的星型结构，中心节点出现故障会导致整个网络瘫痪。（  √ ）

49.在数据传输中，多模光纤的性能要优于单模光纤。（ × ）